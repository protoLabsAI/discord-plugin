# Test-only deps. At runtime the protoAgent host provides langchain-core, fastapi,
# httpx, websockets, pydantic, and the `graph.*`/`infra.*` packages the plugin
# imports lazily; these let the suite run standalone in CI with no host. The plugin
# declares NO runtime pip deps — it relies on the host's core deps.
fastapi>=0.110
langchain-core>=0.2
httpx>=0.27
websockets>=12
pyyaml>=6
pytest>=8
pytest-asyncio>=0.23
